{
  "type": "module",
  "scripts": {
    "prebuild": "wasm-pack build --target web --no-typescript --no-pack --out-dir pkg wasm_proxies",
    "build": "vite build"
  },
  "devDependencies": {
    "rust": "^1.0.8-nightly",
    "vite": "^6.3.5",
    "vite-plugin-html-minifier": "^1.0.5",
    "wasm-pack": "^0.13.1"
  },
  "private": true
}
