{
  "type": "module",
  "main": "assets/interface.js",
  "scripts": {
    "build": "wasm-pack build --target web --no-typescript --no-pack --out-dir pkg wasm_proxies && rollup -c"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "rollup": "^4.41.1",
    "rollup-plugin-copy": "^3.5.0",
    "wasm-pack": "^0.13.1"
  },
  "private": true
}
